__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
.report.json
dist/
build/
*.egg-info/
site/

.cache/
.atf/
.playwright-mcp/
.claude/settings.local.json

# Isolated worktrees for background agents. Never part of the repository.
.claude/worktrees/

# What the example suites make when you run them. The declarations are the repository's; the rows
# and files they provision are not.
examples/**/*.db
examples/shipped/tmp/
atf-docs/
.workspaces/

# Reports the suite's own scenarios write when a run is pointed at the repo root.
out.json
tally.txt
atf-docs/
