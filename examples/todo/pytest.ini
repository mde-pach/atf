[pytest]
# ATF needs no configuration here — it registers itself as a pytest plugin, and `specs/` would be
# collected without this file. It is here for `refused/`, which holds a suite that is wrong on
# purpose and must not be collected with the rest.
testpaths = specs
