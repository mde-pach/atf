[pytest]
# Here so this directory is its own rootdir: `pytest_plugins` in conftest.py is only allowed in the
# top-level one, and the repository's pyproject.toml would otherwise claim that role.
testpaths = .
