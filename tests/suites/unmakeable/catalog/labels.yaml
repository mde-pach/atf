hero:
  resource: label
  represents: Something that depends on what cannot be made, so it is never attempted at all.
  depends_on:
    - badges.missing
  body:
    slug: hero
    owner_id: ${badges.missing.id}

top:
  resource: label
  represents: Two steps from what cannot be made, so blocking has to carry past its direct cause.
  depends_on:
    - labels.hero
  body:
    slug: top
    owner_id: ${labels.hero.id}
